#!/bin/sh
set -e

echo "Running gofmt check..."
UNFORMATTED=$(gofmt -l .)
if [ -n "$UNFORMATTED" ]; then
    echo "Files not formatted:"
    echo "$UNFORMATTED"
    exit 1
fi

echo "Running go vet..."
go vet ./...

echo "Pre-commit checks passed."
